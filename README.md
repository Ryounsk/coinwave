# Coin Wave (币浪)

币浪是一个基于 Golang (Gin) 和 Vue 3 的内容付费社区平台。

## 🐳 Docker 快速启动 (推荐)

如果你本地已经安装了 Docker，可以通过以下步骤一键启动完整环境。

### 1. 准备工作
确保 Docker Desktop 正在运行。

### 2. 停止冲突服务
Docker 容器将占用以下端口，请确保它们未被本地其他服务占用：
- **8080**: 后端 API
- **3000**: 前端访问入口

如果你之前在终端运行了 `go run` 或 `npm run dev`，请按 `Ctrl+C` 停止它们。

### 3. 启动服务
在项目根目录下（即包含 `docker-compose.yml` 的目录），运行以下命令：

```bash
docker compose up --build -d
```

> 如果你的 Docker 版本较旧，可能需要使用 `docker-compose up --build -d`

### 4. 访问项目
启动完成后，浏览器访问：
- **前台页面**: [http://localhost:3000](http://localhost:3000)
- **后端接口**: [http://localhost:8080](http://localhost:8080)

### 5. 停止服务
```bash
docker compose down
```

---

## 🛠 本地开发 (非 Docker)

如果你想在本地调试源码：

### 后端 (Backend)
```bash
cd backend
go mod tidy
go run main.go
# 运行在 http://localhost:8080
```

### 前端 (Frontend)
```bash
cd frontend
npm install
npm run dev
# 运行在 http://localhost:5173
```
