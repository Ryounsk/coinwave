apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: coinwave-backend:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: GIN_MODE
          valueFrom:
            configMapKeyRef:
              name: coinwave-config
              key: GIN_MODE
        - name: DB_DSN
          valueFrom:
            configMapKeyRef:
              name: coinwave-config
              key: DB_DSN_TEMPLATE
        - name: REDIS_ADDR
          valueFrom:
            configMapKeyRef:
              name: coinwave-config
              key: REDIS_ADDR
        - name: MILVUS_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: coinwave-config
              key: MILVUS_ADDRESS
        - name: VOLC_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: coinwave-secrets
              key: volc-auth-token
        - name: VOLC_MODEL_EMBEDDING
          valueFrom:
            configMapKeyRef:
              name: coinwave-config
              key: VOLC_MODEL_EMBEDDING
        - name: VOLC_MODEL_LLM
          valueFrom:
            configMapKeyRef:
              name: coinwave-config
              key: VOLC_MODEL_LLM
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /api/rates
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /api/rates
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: backend
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
